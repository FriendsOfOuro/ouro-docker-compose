version: '2'

services:

  eventstore:
    build:
      context: ./build
      args:
        ES_VERSION: 4.1.1-1
    volumes:
      - "./data/eventstore:/var/lib/eventstore"
    ports:
     - "2113:2113"
     - "1113:1113"
